import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false, // disables Fast Refresh and build indicators
  reactStrictMode: true, // optional, just keep good practice
};

export default nextConfig;
